# n8n


Daily if still using cloudflare.

cloudflared tunnel --url http://localhost:5678 (to get a fresh tunnel for server)

docker compose down
docker compose up -d (to restart the docker container)
